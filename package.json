{
    "scripts": {
      "api": "gunicorn --chdir backend flaskr:app",
      "heroku-postbuild": "cd frontend && npm install && npm run build:staging"
    },
    "cacheDirectories": [
      "frontend/node_modules"
    ]
  }